spring.application.name=ai-ticket
spring.mvc.servlet.load-on-startup=1

# DataSource Configuration
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:ai_ticket}
spring.datasource.username=${DB_USERNAME:sa}
spring.datasource.password=${DB_PASSWORD:p@ssw0rd}
spring.datasource.driver-class-name=org.postgresql.Driver

# HikariCP
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=20000

# JPA
spring.jpa.hibernate.ddl-auto=validate
# spring.jpa.show-sql=true

# JWT
jwtSecret=fU+T0p6YwFv3L/7Y8D6f/S3ZonWk9iU+UfGzR0sW7XU=
jwtExpirationMs=86400000


